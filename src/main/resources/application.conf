# Default configuration values
http {
  host = "0.0.0.0"
  port = 8080
}

database {
  url = "jdbc:postgresql://localhost:5432/orders"
  user = "orders"
  password = "orders"
  driver = "org.postgresql.Driver"
  maxPoolSize = 10
}